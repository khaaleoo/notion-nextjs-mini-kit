import { FC } from "react";

type TProps = {};
const Page: FC<TProps> = (props: TProps) => {
  return <main className="flex min-h-screen flex-col items-center justify-between p-24">Hello World</main>;
};

export default Page;
